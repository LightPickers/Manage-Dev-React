import React from "react";
import ReactDOM from "react-dom/client";

import LightPickersAdminApp from "@/LightPickersAdminApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LightPickersAdminApp />
  </React.StrictMode>
);
